# Deployment

| Folder | Purpose |
|---|---|
| `docker/` | Dockerfiles for backend and frontend |
| `nginx/` | Reverse-proxy config for production |
| `github-actions/` | Reference copies / notes for CI workflows (source of truth is `.github/workflows/`) |

Local orchestration is via the root `docker-compose.yml`. Production deployment
details will be documented here once the deployment phase begins.
