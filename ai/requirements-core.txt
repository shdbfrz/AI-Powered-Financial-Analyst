# Dependencies needed by ai/data_collection/ (Sprint 1), ai/feature_engineering/
# (Sprint 2), and ai/models/ml/ (Sprint 3).
#
# ai/requirements.txt contains the FULL future dependency set for all
# planned ai/ modules (ML, time series, deep learning, NLP), including
# packages not used yet. As of this writing it has an unresolved conflict
# (numpy==2.1.1 vs tensorflow==2.17.0's numpy<2.0.0 requirement) that has
# nothing to do with Sprints 1-3 and will be fixed when Sprint 5 (Deep
# Learning) is actually implemented and those pins are revisited.
#
# CI's ai-tests job installs from THIS file so Sprints 1-3's tests aren't
# blocked by dependency conflicts in code that isn't written yet.
pandas==2.2.3
numpy>=1.26.4,<3.0.0
requests==2.32.3
yfinance==1.5.1
pydantic-settings==2.5.2
pytest==8.3.3

# --- Sprint 3 (ai/models/ml/) ---
scikit-learn==1.5.2
xgboost==2.1.1
joblib==1.4.2
matplotlib==3.9.2
seaborn==0.13.2
tabulate==0.9.0
# LightGBM/CatBoost are explicitly optional per the Sprint 3 spec: the model
# registry (ai/models/ml/models/registry.py) detects their absence at import
# time and simply omits them rather than failing, so CI stays green even if
# one of these two fails to install on a given runner/platform.
lightgbm==4.5.0
catboost==1.2.7