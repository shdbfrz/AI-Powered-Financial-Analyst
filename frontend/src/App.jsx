/**
 * Root component. Routing (React Router) and page components will be wired
 * in here once the auth and dashboard pages are implemented (Phase 1
 * frontend skeleton step).
 */
function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-950 text-slate-100">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">AI-Powered Financial Analyst</h1>
        <p className="text-slate-400 mt-2">Scaffold ready — dashboard coming in a later phase.</p>
      </div>
    </div>
  );
}

export default App;
