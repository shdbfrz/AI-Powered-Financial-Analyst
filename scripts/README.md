# Scripts

One-off / operational scripts (data backfills, DB seeding, model retraining
triggers, cron jobs). Each script should be runnable standalone with a
docstring header explaining purpose, usage, and required env vars.
