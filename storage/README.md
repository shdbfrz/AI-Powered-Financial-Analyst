# Storage

Runtime artifacts, not version-controlled.

| Folder | Purpose |
|---|---|
| `models/` | Trained model artifacts (`.pkl`, `.h5`, `.pt`) |
| `reports/` | Generated PDF/HTML reports |
| `logs/` | Application and training logs |
| `cache/` | Cached API responses / intermediate computations |
